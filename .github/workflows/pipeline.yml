name: Flask Ecommerce CI/CD Pipeline

on:
  push:
    branches: [ "dev", "prod" ]
  pull_request:
    branches: [ "dev", "prod" ]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Pytest
      run: |
        pytest --verbose

  deploy-dev:
    name: Deploy to Development
    needs: test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/dev')
    runs-on: ubuntu-latest
    steps:
    - name: Dev Deployment Placeholder
      run: |
        echo "Tests passed! Deploying to the DEVELOPMENT server..."
        # Add your Dev deployment commands here (e.g. Render, Heroku, or SSH)

  deploy-prod:
    name: Deploy to Production
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/prod'
    runs-on: ubuntu-latest
    steps:
    - name: Production Deployment Placeholder
      run: |
        echo "Tests passed! Triggering PRODUCTION deployment..."
        # Add your Production deployment commands here (e.g. AWS, DigitalOcean)
